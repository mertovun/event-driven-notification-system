// Command notifyd is the event-driven notification system service binary.
// It runs the HTTP API and worker pools as a single process by default;
// see --mode for split-mode operation.
package main

func main() {}
